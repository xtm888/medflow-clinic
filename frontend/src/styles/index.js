/**
 * Styles Index
 *
 * Centralized export for all design tokens and style utilities.
 *
 * Usage:
 * import { colors, spacing, shadows } from '@/styles';
 */

export {
  colors,
  spacing,
  typography,
  shadows,
  borderRadius,
  zIndex,
  breakpoints,
  transitions,
  columnLayouts,
  componentSizes,
  withOpacity,
  getColumnStyle,
  default as designTokens,
} from './designTokens';
